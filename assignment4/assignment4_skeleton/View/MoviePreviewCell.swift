import SwiftUI
import Kingfisher

struct MovieCell: View {
    
    let movie: Movie
    
    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Hello world!")
        }
        .padding(.bottom, 16)
    }
}
